import djikstra_algo as dji
import networkx as nx

class RecursiveGraph
    def __init__(self, graph, command,baseCutoff):
        self.baseObjectGraph = graph
        self.command = command
        self.baseCutoff = baseCutoff
        self.actionCostGraph = nx.graph()
        self.actionCostGraph.add_node(0)


    def recursiveGraphMaker(self, position, ):
        #on part de la node actuelle






